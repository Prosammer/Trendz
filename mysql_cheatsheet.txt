# Login:
mysql -u root -p
Jura55ic5
# shows first 10 records
select * from keywords order by 'index' limit 10;

# Count number of rows
SELECT COUNT(*) FROM keywords;


#Duplicate finder:
SELECT topic_title, COUNT(*) FROM keywords GROUP BY topic_title HAVING COUNT(*) > 1;

# Duplicate deleter:
ALTER IGNORE TABLE keywords ADD UNIQUE INDEX idx_name (topic_title, topic_type);

# Selecting simlar text:
SELECT * FROM keywords WHERE topic_title LIKE '%dict%';